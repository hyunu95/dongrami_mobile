document.addEventListener('DOMContentLoaded', () => {
    const nicknameElement = document.getElementById('nickname');
    const userNickname = '동그라미'; // 여기에 회원가입 시 사용자가 입력한 닉네임을 넣어주세요.
    nicknameElement.textContent = userNickname;
});
